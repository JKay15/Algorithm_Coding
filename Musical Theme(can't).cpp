//感觉要用某种复杂的算法，因为数据量大到只有nlogn以下才行
#include <bits/stdc++.h>
using namespace std;
int music[20005];
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    while(cin>>n&&n){
        for(int i=0;i<n;i++)cin>>music[i];
        
    }

    return 0;
}